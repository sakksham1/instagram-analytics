/** App-wide constants. Nothing environment- or backend-specific belongs
 * here — there is no backend. */
export const MAX_EXPORT_SIZE_BYTES = 500 * 1024 * 1024; // 500MB, generous for large accounts
export const APP_NAME = "instagram-analytics";
