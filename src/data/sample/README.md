Small hand-written fixtures matching the real Instagram v1 export shape.
Used by parser/analytics tests and for local UI development without
needing a real export.

- `alice` — mutual (in both files)
- `bob`, `carol` — follow the user, not followed back
- `dave` — followed by the user, doesn't follow back

See `src/parser/versions/v1/index.test.ts`.
