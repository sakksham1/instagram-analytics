// Global Vitest setup. Currently a no-op — the initial test suite covers
// pure parser/analytics/util functions and doesn't need DOM matchers.
// Add `@testing-library/jest-dom` here if/when component tests are added.
export {};
